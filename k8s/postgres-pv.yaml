apiVersion: v1
kind: PersistentVolume
metadata: 
  name: postgres-pv
  labels:
    app: postgres
    env: production
spec:
  capacity:
    storage: 1Gi
  accessModes: 
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath: 
    path: /data/postgres/production1
